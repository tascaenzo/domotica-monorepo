import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabContainer: {
    justifyContent: 'space-around',
    flexDirection: "row",
    paddingBottom: 15,
  },

  pushContainer: {
    backgroundColor: '#FFF',
    height: 85,
    borderRadius: 30,
    margin: 8
  }
});
