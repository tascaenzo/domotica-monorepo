import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 50,
    margin: 12,
    padding: 10,
    fontFamily: 'OxygenLight',
    backgroundColor: '#FFF',
    borderRadius: 15,
  },
});
